import express from "express";
import dotenv from "dotenv";
import TelegramBot from "node-telegram-bot-api";

dotenv.config();
const app=express();



export function str5(bot) {
    bot.on("message", (msg) => {
        const chatId = msg.chat.id;
        const text = msg.text;   
              
    if(text === "69"){
        bot.sendMessage(chatId,
            "2str"
        )
}   
    if(text === "70"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "71"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "72"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "73"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "74"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "75"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "76"){
        bot.sendMessage(chatId,
            ""
        )
} 
    if(text === "77"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "78"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "79"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "80"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "81"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "82"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "83"){
        bot.sendMessage(chatId,
            ""
        )
}   
    if(text === "84"){
        bot.sendMessage(chatId,
            ""
        )
} 
    if(text === "85"){
        bot.sendMessage(chatId,
            ""
        )
}  
})
}
